# Корпоративный таск-менеджер: Telegram-бот + веб-кабинет

> Предварительный продуктовый и технический дизайн.
> Аналог Asana по функциям, скорость и ощущение интерфейса — как Linear.
> Главный принцип: **Telegram заменяет мобильное приложение**, а сайт — это «толстый» рабочий кабинет.

---

## 0. Зафиксированные решения по скоупу (обновлено)

- **Single-tenant**: система только для одной компании. Никакого мультитенанта, переключения организаций и тарифов. Одна `Organization` зашита в конфиг.
- **Часовой пояс**: единый для всех — **Europe/Moscow**. Логики «TZ на пользователя» нет; все даты/напоминания/повторения считаются по МСК.
- **Дизайн/UI**: ориентир — **ClickUp** (структура и плотность), ощущение работы — Asana/ClickUp + скорость Linear.
- **Скоуп веб-сайта в MVP**:
  1. **Страница авторизации** (вход через Telegram).
  2. **Рабочая страница проектов/процессов** (список проектов/процессов + доска задач: канбан/список, карточка задачи).
  3. **Функциональная схема** — дерево функций компании; к функциям прикрепляются задачи, а уже потом назначается исполнитель.
- Всё остальное (карточки сотрудников, отдельный календарь, платёжный календарь, LLM-чат) — **за рамками MVP**, в бэклоге (см. §11).
- Telegram-бот остаётся полноценным каналом (создание задач, напоминания, выполнение с доказательством, комментарии) — он и есть «мобильное приложение».

### Роли и видимость (зафиксировано)
- **Главный администратор** (один — владелец): видит и делает всё.
- **Руководитель процесса**: видит **все задачи внутри своего процесса/проекта**, управляет ими.
- **Сотрудник (member)**: видит **только свои задачи** — даже если состоит в совместном проекте. Чужие задачи того же проекта ему не видны.
- **Участник задачи**: к любой задаче можно подключить доп. участника (watcher/соисполнитель) — он получает доступ к этой конкретной задаче.

### Кто может ставить задачи (зафиксировано)
- Задачу **в процесс / проект / функцию** может поставить только **админ** или **руководитель** (процесса).
- Задачу **себе** может поставить **любой сотрудник**.

### «Мои задачи» — что туда попадает (зафиксировано)
- Задачи, где я **исполнитель**;
- задачи, которые мне **поставили** другие (делегированные мне);
- задачи, где я **участник**.
- То есть это всё, к чему я причастен как исполнитель/получатель, а не только формально назначенные.

### Функциональная схема — ядро модели (зафиксировано)
- Управление идёт **от функций, а не от людей**: администратор отвечает за функции и постановку задач в них, и лишь затем назначает задачу на сотрудника.
- Есть **дерево функций** компании. Задача **прикрепляется к функции**, после чего назначается исполнитель.
- Сотрудник может быть «привязан» к функции, но первичен узел функции, а не человек.

### Доказательство выполнения (зафиксировано)
- **Свободный формат**: скриншот / текст / ссылка (или их комбинация).
- Требуется **не всегда**, а только если администратор/руководитель включил флаг `proof_required` для задачи.
- Если флаг включён — **задача не может быть закрыта (перейти в `done`), пока не приложено доказательство**.
- Файлы хранятся **на нашем сервере** (self-hosted, см. §4).

### Статусы задач и цвета (зафиксировано)
| Статус | Код | Цвет |
|--------|-----|------|
| Взять в работу | `to_do` | серо-голубой |
| В работе | `in_progress` | синий |
| Отложено | `on_hold` | жёлтый |
| Возникли трудности | `blocked` | оранжевый |
| Не выполнено | `failed` | красный |
| Выполнено | `done` | зелёный |
| Отменено | `canceled` | серый |

- **Просрочено** — это не статус, а **производный признак** (`due_at < now` и задача не в `done`). Поэтому задача может быть «в работе» и одновременно «просрочена» — важно для отчётов.

### Повторение процессов (зафиксировано)
- У процесса/задачи может быть **любое повторение** из доступных: день / неделя / месяц / квартал / конкретная дата (или разовая, без повтора).
- Принцип: в компании есть **постоянные** (повторяющиеся) и **переменные** (разовые) задачи — система поддерживает оба типа в одном механизме.

### Связь с Telegram на уровне проекта (зафиксировано, ориентир — UTasks)
- Проектом/процессом можно сделать **группу или канал Telegram**: обсуждение задач ведётся прямо в Telegram, а сообщения связываются с задачами.
- Комментарии к задаче синхронизируются между сайтом и Telegram-тредом.

---

## 1. Продуктовое видение

Единая система управления задачами компании с двумя равноценными точками входа:

- **Telegram-бот** — основной канал для исполнителей. Создание задач, напоминания, отметка о выполнении, комментарии, прикрепление доказательств — всё из чата. Авторизация на сайте тоже через Telegram.
- **Веб-кабинет** (ClickUp-стиль) — вход, проекты/процессы (канбан/список + карточка), функциональная схема, входящие, отчёты. Остальные модули — позже.

Данные общие: одна задача, созданная в боте, мгновенно видна на сайте и наоборот (real-time).

### Ключевое архитектурное решение
Бот и сайт — это **две «головы» к одному бэкенду**. Никакой логики «только в боте» или «только на сайте»: всё проходит через единый API/доменный слой.

---

## 2. Карта требований → фичи

| # | Требование | Где живёт | Приоритет |
|---|-----------|-----------|-----------|
| 1 | Создание задач | Бот + Веб | MVP |
| 2 | Запрет удаления (только архив/отмена со следом) | Ядро | MVP |
| 3 | Повторение: день / неделя / месяц / квартал / конкретная дата | Ядро (recurrence engine) | MVP |
| 4 | Авторизация на сайте через Telegram | Auth | MVP |
| 5 | Напоминания за 1 час и за 1 день | Scheduler | MVP |
| 6 | Назначение исполнителя | Ядро | MVP |
| 7 | Доказательство выполнения (скрин/текст/ссылка, блокирует закрытие) | Storage + Задача | MVP |
| 8 | Проекты / процессы (любое повторение: день/неделя/месяц/квартал/дата) | Ядро | MVP |
| 9 | Функциональная схема + прикрепление задач к функциям | Веб | **MVP** |
| 10 | Карточка сотрудника со списком задач | Веб | v1.2 |
| 11 | Карточки задач: список / канбан | Веб | MVP |
| 12 | Раздел «Мои задачи» | Бот + Веб | MVP |
| 13 | Календарь событий (задача со временем) | Веб + Бот | v1.1 |
| 14 | Ориентир по UI — ClickUp / Asana | — | — |
| 15 | Тесная связь с Telegram | Вся система | MVP |
| 16 | Комментарии | Ядро | MVP |
| 17 | Приоритеты: высокий / средний / низкий / супер-сложно | Ядро | MVP |
| 18 | Платёжный календарь | Веб (модуль) | v1.2 |
| 19 | Общение с LLM внутри сайта | Веб (AI) | v1.2 |
| 20 | Мобильная версия сайта | Веб (responsive/PWA) | MVP |
| 21 | Статусы задач с цветами (взять в работу / в работе / отложено / трудности / не выполнено) | Ядро | MVP |
| 22 | Отчёты по задачам (выполнено / просрочено / в работе) + кто просрочивает | Веб (Reports) | MVP |
| 23 | Сортировка (приоритет / дата / сотрудник) | Веб | MVP |
| 24 | Поиск по задачам + фильтры (как Asana) | Веб + Бэк | MVP |
| 25 | Подзадачи (у задачи могут быть подзадачи) | Ядро | MVP |
| 26 | Кастомные поля задач | Ядро | MVP |
| 27 | Раздел «Входящие» (уведомления о причастных задачах, как Asana) | Веб + Бот | MVP |
| 28 | Генерация/улучшение описания + разбивка на подзадачи через **ChatPRD** | Веб (AI) | v1.1 |
| 29 | Проект = Telegram-группа/канал, обсуждение в Telegram (как UTasks) | Бот + Ядро | v1.1 |
| 30 | Функциональная схема по образцу **kvant.app** (ОФС: функции, рез-т, исполнители) | Веб | MVP |

---

## 3. Доменная модель (сущности)

```
Organization (компания — одна, зашита)
 ├── User (сотрудник; привязан к Telegram ID)
 ├── Function (узел ФУНКЦИОНАЛЬНОЙ схемы, дерево) ← ядро управления
 ├── Project / Process (проект ИЛИ процесс; может быть привязан к TG-группе/каналу)
 │     ├── lead (руководитель процесса — видит все задачи внутри)
 │     ├── CustomFieldDef[] (определения кастомных полей)
 │     └── Task
 │           ├── function_id  → к какой функции прикреплена задача
 │           ├── parent_task_id → ПОДЗАДАЧИ (дерево задач)
 │           ├── Assignee (User)            — назначается ПОСЛЕ выбора функции
 │           ├── Participant[] (доп. участники задачи)
 │           ├── CustomFieldValue[] (значения кастомных полей)
 │           ├── Recurrence (правило повторения)
 │           ├── Reminder[] (за 1ч / за 1д / кастом)
 │           ├── Comment[] (синхронизируются с TG-тредом)
 │           ├── Attachment[] / CompletionProof (скрин/текст/ссылка)
 │           └── ActivityLog[] (неизменяемый журнал)
 ├── Notification (для раздела «Входящие»)
 └── PaymentEvent (для платёжного календаря, бэклог)
```

### 3.1 User
- `telegram_id` (главный идентификатор для входа), `telegram_username`
- ФИО, должность, аватар
- `role`: `super_admin` (один, владелец) | `process_lead` | `member`
- `function_ids[]` — к каким функциям привязан (вторично; первична функция)
- статус (активен / в отпуске / уволен — без удаления)

### 3.1a Function (узел функциональной схемы) ← ключевая сущность
> Модель — как ОФС в **kvant.app («Квант»)**: функции распределены по подразделениям, на функции «расставлены» сотрудники, у функции есть ожидаемый результат и повторяющиеся задачи. Схема = центр управления; обязанности закреплены за функцией, а не за человеком (ушёл сотрудник — функция и её задачи остаются в системе).

- `name`, `description`
- `parent_id` — дерево функций (через `ltree` / recursive CTE в Postgres). Узлы: подразделение → функция → подфункция.
- `expected_result` — **ожидаемый результат функции** (ценный конечный продукт), как в Кванте.
- `responsible_user_id` (nullable) — кто сейчас «стоит на функции» (может меняться, не влияя на функцию).
- К функции **прикрепляются задачи** (`Task.function_id`), в т.ч. повторяющиеся. Логика постановки: выбрал функцию → поставил задачу → назначил исполнителя.
- **Функции создаёт сам администратор** (вручную строит дерево). Преднаполнения нет.
- Удаление запрещено (архив), как и у задач.

### 3.2 Project / Process (проект или процесс)
- `type`: `project` | `process`
  - **project** — обычный набор задач с дедлайнами.
  - **process** — проект повторяющихся операций. Повторение **любое** (день/неделя/месяц/квартал/дата). Создаётся шаблон → воркер заранее «материализует» экземпляры задач. Поддерживает как постоянные (повторяющиеся), так и переменные (разовые) задачи.
- `lead_id` — **руководитель процесса** (видит все задачи внутри, управляет ими).
- `telegram_chat_id` (nullable) — привязка к группе/каналу Telegram (стиль UTasks): обсуждение задач идёт в этом чате.
- участники, цвет, статус (активен/архив).

### 3.3 Task
- `title`, `description`
- `project_id`, **`function_id`** (к какой функции прикреплена)
- `parent_task_id` (nullable) — **подзадачи**: задача может иметь вложенные подзадачи (дерево). Прогресс родителя считается по закрытым подзадачам.
- `assignee_id`, `creator_id`, `participant_ids[]`
- `priority`: `low` | `medium` | `high` | `critical` («супер-сложно»)
- `status`: `to_do` | `in_progress` | `on_hold` | `blocked` | `failed` | `done` | `canceled` (с цветами, см. §0)
- `is_overdue` — **производное** (`due_at < now` и не `done`), не хранимый статус.
- `due_at` (дата+время по МСК)
- `recurrence_id` (nullable)
- `custom_fields` — значения кастомных полей (см. §3.8)
- `is_deletable = false` — **удаление запрещено на уровне домена**. Доступны только: терминальные статусы (`done`/`failed`/`canceled` с причиной) и `archive`. Любое действие пишется в `ActivityLog`.
- **`proof_required: bool`** — ставит администратор/руководитель. Если `true`, переход в `done` **блокируется**, пока не приложен `CompletionProof`.
- `completion_proof` — свободный формат: текст и/или ссылка и/или файл-скриншот.

### 3.3a Видимость задач (правила доступа)
- `super_admin` — видит все задачи.
- `process_lead` — видит все задачи **в своих процессах/проектах**.
- `member` — видит **только задачи, где он `assignee`, `creator`-получатель или `participant`** (даже внутри общего проекта чужие задачи скрыты).
- **Кто может ставить задачи**: в процесс/проект/функцию — только `super_admin`/`process_lead`; **себе** — любой сотрудник.
- Реализуется фильтрацией в сервисном слое (а не только на фронте) + проверкой на каждом API-эндпоинте.

### 3.4 Recurrence (движок повторений)
Правило в духе RRULE (iCalendar):
- `daily` / `weekly` / `monthly` / `quarterly` / `on_date(specific date)`
- `interval`, `by_weekday`, `by_monthday`, `time_of_day`, `until` / `count`
- Реализация: **шаблон задачи** + воркер, который заранее (например, на 30 дней вперёд) создаёт конкретные экземпляры. Это даёт корректный календарь и напоминания без «магии на лету».

### 3.5 Reminder
- `offset`: `-1h`, `-1d` (плюс кастомные в будущем)
- канал: Telegram (push на сайте/PWA — позже)
- статус: scheduled / sent / dismissed

### 3.6 Attachment / CompletionProof (доказательство выполнения)
- **Свободный формат**: `text` (комментарий), `link` (ссылка), `file` (скриншот/документ) — можно сразу несколько.
- В Telegram — прямо из чата (фото/файл/текст/ссылка); на сайте — drag&drop / вставка ссылки / текст.
- `kind`: `attachment` (обычное вложение) | `completion_proof` (доказательство закрытия).
- Бизнес-правило: если у задачи `proof_required = true`, переход в `done` запрещён без хотя бы одного `completion_proof`.
- Хранение — **на нашем сервере** (self-hosted MinIO или локальная папка за обратным прокси), без внешних облаков.

### 3.7 ActivityLog (журнал, неизменяемый)
Так реализуется «невозможность удалить»: ничего не пропадает, всё — append-only. Кто, что, когда изменил; смена статусов, исполнителя, комментарии.

### 3.8 Поля задачи: базовые + кастомные
**Базовые поля** (встроенные, всегда видны в карточке и колонками в списке — без преднастройки):
- **Приоритет** (низкий/средний/высокий/супер-сложно)
- **Исполнитель** — с **аватаркой**
- **Статус задачи** (с цветом, см. §0)
- **Срок выполнения** (`due_at`)

**Кастомные поля** (опционально, по необходимости — дополнительных полей по умолчанию НЕ заводим):
- `CustomFieldDef` — определение на уровне проекта/процесса: `name`, `type` (`text` | `number` | `date` | `select` | `url`/«сайт» | `user`/«участник» | `checkbox`).
- `CustomFieldValue` — значение на конкретной задаче.
- Примеры при необходимости: «Сайт» (url), «Участник» (user). Поля настраивает администратор.

### 3.9 Notification + Inbox («Входящие», как в Asana)
- `Notification`: `user_id`, `task_id`, `type` (назначен / новый комментарий / смена статуса / приближается срок / упомянут / добавлен участником), `is_read`, `created_at`.
- Раздел **«Входящие»** показывает все уведомления о задачах, к которым пользователь причастен (назначен, участник, создатель, упомянут).
- Дублируется в Telegram: те же события приходят сообщениями.

### 3.10 Comment
- `task_id`, `author_id`, `body`, вложения.
- При наличии `telegram_chat_id` у проекта — **двусторонняя синхронизация** с тредом в Telegram (стиль UTasks).

---

## 4. Технологический стек («шустрый как Linear»)

Ощущение скорости Linear = **локальный кэш + оптимистичные обновления + real-time синхронизация**, а не «дёргать сервер на каждый клик».

### Frontend (Веб + мобильный)
- **React + TypeScript + Vite**
- **TanStack Query** + оптимистичные мутации (UI отвечает мгновенно, синхронизация — в фоне)
- **Zustand** для локального состояния
- **Tailwind CSS** + shadcn/ui — быстрый, аккуратный UI
- **Real-time**: WebSocket (см. бэкенд) → мгновенное появление чужих изменений
- **PWA** (offline-кэш, установка на телефон, push) → закрывает «мобильную версию»
- Виртуализация списков (react-virtual) для больших досок

### Backend
- **Node.js (NestJS) + TypeScript** — единый язык с фронтом, быстрый DX
  - *(альтернатива: Go/Fastify, если нужен максимум по латентности)*
- **PostgreSQL** — основная БД (надёжные транзакции, **дерево функциональной схемы** через `ltree`/recursive CTE)
- **Redis** — кэш, очереди, pub/sub для real-time
- **BullMQ** (на Redis) — отложенные напоминания и материализация повторяющихся задач
- **WebSocket-шлюз** (Socket.IO / native ws) — real-time апдейты на сайт
- **Хранилище файлов — на нашем сервере**: self-hosted **MinIO** (S3-совместимый, но локально) или файловая папка за обратным прокси. Без внешних облаков.

### Telegram-бот
- **grammY** (TS) или **aiogram** (Python) — webhook-режим
- Тот же бэкенд-API; бот — тонкий клиент над доменным слоем
- Inline-кнопки, deep links, file upload, mini-app (Telegram Web App) для сложных экранов

### LLM / AI
- **ChatPRD** — для генерации и улучшения описаний задач и разбивки на подзадачи (как `@chatprd` в Linear). Подключение через API ChatPRD (Enterprise) / MCP; передаём контекст задачи → получаем описание/подзадачи. См. §8a.
- Базовый LLM (Anthropic Claude) — для бота (разбор естественного языка при создании задач) и будущего чата на сайте, стриминг ответов, доступ к контексту через function calling.

### Инфраструктура
- Docker + docker-compose (dev), деплой — Fly.io / Railway / VPS + Caddy
- Миграции — Prisma / Drizzle
- Мониторинг — Sentry + простой uptime-чек

---

## 5. Авторизация через Telegram

Два сценария — оба официальные и безопасные:

1. **Telegram Login Widget** на сайте: пользователь жмёт «Войти через Telegram», подтверждает в приложении, сайт получает подписанные данные (проверяем HMAC по bot token). По `telegram_id` находим/создаём `User`.
2. **Из бота** (`/login`): бот присылает кнопку с одноразовой magic-link / кодом, по которому сайт логинит.

После входа — сессия через JWT (access + refresh) или httpOnly-cookie. Привязка `telegram_id` — это «единый аккаунт» для бота и сайта.

---

## 6. UX: экраны веб-кабинета

> UI-ориентир — **ClickUp / Asana**: левый сайдбар с иерархией, плотный список/канбан в центре, выезжающая карточка задачи справа. Плюс скорость и горячие клавиши Linear.

### MVP — три экрана

**A. Страница авторизации**
- Логотип, кнопка «Войти через Telegram» (Login Widget).
- После входа — редирект на рабочую страницу. Сессия в httpOnly-cookie.
- Ничего лишнего: один экран, один путь.

**B. Рабочая страница проектов/процессов** (главный экран, в стиле ClickUp)
- **Левый сайдбар**: список проектов и процессов (процесс помечен иконкой повторения), пункты «Входящие», «Мои задачи», «Функциональная схема», «Отчёты».
- **Центр**: доска задач выбранного проекта с переключателем вида:
  - **Канбан** — колонки по статусам с цветами (`Взять в работу → В работе → Отложено → Возникли трудности → Выполнено`, плюс «Не выполнено»/«Отменено»), drag&drop, оптимистичный UI.
  - **Список** — плотная таблица, инлайн-редактирование, колонки кастомных полей.
- **Поиск и фильтры (как в Asana)**: глобальный поиск по задачам; фильтры по статусу, исполнителю, приоритету, функции, проекту, сроку (просрочено/сегодня/неделя), кастомным полям. Сохраняемые представления.
- **Сортировка**: по приоритету / по дате (сроку) / по сотруднику.
- **Видимость по ролям** (§3.3a): сотрудник видит на доске только свои задачи; руководитель процесса — все задачи процесса; главный админ — всё.
- **Быстрое создание** — строка вверху доски + `Cmd+K`. При создании: проект → **функция (из схемы)** → исполнитель → приоритет → срок → повтор → флаг «нужно доказательство». Кнопка **«Сгенерировать описание (ChatPRD)»**.
- **Карточка задачи** (выезжает справа): описание (+генерация/улучшение через **ChatPRD**, разбивка на подзадачи), **функция**, исполнитель (с аватаркой) и участники, приоритет, статус, срок+время, повторение, напоминания, **подзадачи** (чек-лист с прогрессом), **кастомные поля**, комментарии, вложения и **доказательства выполнения** (текст/ссылка/скрин), журнал. Кнопки `Сменить статус` / `Выполнить` / `Перенести` / `В архив` (удаления нет). Если `proof_required` — `Выполнить` неактивна, пока нет доказательства.

**C. Функциональная схема** (ядро управления)
- **Дерево функций** компании (раскрывающиеся узлы, drag&drop для переноса).
- У каждого узла: ответственный (опционально) и **список прикреплённых задач** с их статусами.
- Из узла можно сразу **поставить задачу в функцию** → затем назначить исполнителя.
- Это управленческий вид «от функций, а не от людей».

**D. Входящие** (как в Asana) — лента уведомлений о задачах, к которым я причастен (назначен / участник / создатель / упомянут / новый комментарий / смена статуса / приближается срок). Отметка «прочитано», переход к задаче.

**E. Отчёты** — см. §6a.

UX-принципы: клавиатурная навигация, command palette, оптимистичный UI, минимум модалок, тёмная тема, адаптив/PWA для телефона.

### Позже (бэклог, за рамками MVP)
- Отдельный «Календарь событий», карточка сотрудника, платёжный календарь, AI-чат. Доменная модель уже учитывает их, чтобы не переделывать БД.

---

## 6a. Отчёты (Reports)

Отдельный раздел с аналитикой по задачам.

- **Отчёт по статусам**: сколько задач **выполнено / в работе / просрочено / отложено / не выполнено** — за период, по проекту/процессу/функции.
- **Кто просрочивает**: рейтинг сотрудников по количеству и доле просроченных задач (`due_at < now`, не закрыта). Помогает видеть «узкие места».
- **Производительность**: выполнено за период по сотруднику / функции / процессу.
- **Разрезы (группировки)**: по сотруднику, по функции, по проекту/процессу, по приоритету.
- **Фильтры периода**: сегодня / неделя / месяц / квартал / произвольный диапазон (по МСК).
- Экспорт (CSV) и виджеты на дашборде. Видимость отчётов уважает роли (сотрудник — только свои данные; руководитель — свой процесс; админ — всё).

---

## 7. UX: Telegram-бот (основные сценарии)

Бот **дублирует функционал сайта в упрощённом виде** под Telegram.

### Главное меню (кнопки)
- 📋 **Мои задачи**
- 💬 **Комментарий**
- ➕ **Создать задачу** (себе — любой сотрудник; в проект/функцию — админ/руководитель)
- 📥 **Входящие** (уведомления)

### Действия по задаче (инлайн-кнопки в карточке)
- ✅ **Выполнено** (если `proof_required` — сначала попросит доказательство)
- 🔄 **Сменить статус** → выбор из: Взять в работу / В работе / Отложено / Возникли трудности / Не выполнено
- 📅 **Перенести** (изменить срок)
- 💬 **Комментарий**
- 📎 **Доказательство** (текст / ссылка / фото)
- ⏰ **Напомнить позже**

### Прочее
- `/start` → регистрация/привязка аккаунта.
- **Создать задачу** мастером из кнопок (проект → функция → исполнитель → приоритет → срок → повтор → нужно ли доказательство) или естественным языком (парсим через LLM).
- **Напоминания**: бот сам пишет за 1 день и за 1 час до `due_at` (по МСК). Кнопки прямо в напоминании.
- **Доказательство**: пользователь присылает текст/ссылку/фото в ответ → крепится как `completion_proof`.
- **Комментарии**: ответом на сообщение задачи; синхронизируются с сайтом.
- **Проект = группа/канал Telegram** (стиль **UTasks**): можно привязать процесс/проект к чату, обсуждение задач ведётся прямо там, сообщения связываются с задачами. Команды бота работают и в группе.
- Сложные экраны (канбан, отчёты, календарь) — через **Telegram Mini App** (тот же React, внутри Telegram).

---

## 8. Платёжный календарь (модуль)

- Сущность `PaymentEvent`: контрагент, сумма, валюта, дата платежа, статус (запланирован/оплачен/просрочен), привязка к проекту/задаче.
- Может быть **повторяющимся** (тот же recurrence-движок): аренда раз в месяц, налоги раз в квартал.
- Напоминания о платежах в Telegram (за 1 день/1 час) — переиспользуем scheduler.
- Представление — календарь + список с суммами по периодам (план/факт).

---

## 8a. Интеграция с ChatPRD (AI для задач)

[ChatPRD](https://www.chatprd.ai/) — AI-копайлот для продукта. Используем его как AI-движок для работы с задачами (по образцу его интеграции с Linear, где `@chatprd` улучшает описания и бьёт работу на подзадачи).

**Что делает в нашей системе:**
- **Сгенерировать/улучшить описание задачи** — по короткому заголовку или паре слов ChatPRD пишет нормальное описание. Кнопка в карточке задачи и на сайте, и (упрощённо) в боте.
- **Разбить на подзадачи** — из крупной задачи предлагает список подзадач (мы создаём их как `parent_task_id` детей).
- Возможный триггер в стиле Linear: упоминание `@chatprd` в комментарии задачи → ответ/доработка.

**Подключение:**
- Через **API ChatPRD** (доступ на тарифе Enterprise) или **MCP**-интеграцию. Передаём контекст (заголовок, проект, функцию) → получаем текст/подзадачи.
- Вызовы — на бэкенде (ключ хранится на сервере), результат прилетает в карточку через обычный API + real-time.
- Фолбэк: если ChatPRD недоступен, используем базовый Claude.

> Уточнить: тариф/способ доступа к ChatPRD (Enterprise API vs MCP) и лимиты — см. §12.

---

## 9. Как реализуется «нельзя удалить»

- В домене нет операции `delete` для `Task`/`Project`/`PaymentEvent`.
- Вместо удаления: `cancel` (с обязательной причиной) или `archive`.
- Всё пишется в `ActivityLog` (append-only). Физическое удаление возможно только миграцией БД администратором — не из интерфейса.
- На уровне БД — мягкая блокировка (нет DELETE-эндпоинтов; права через RLS-подобную проверку в сервисном слое).

---

## 10. Real-time и скорость

- Любое изменение → запись в БД → событие в Redis pub/sub → рассылка по WebSocket подписчикам проекта/пользователя.
- Фронт применяет изменение **оптимистично** ещё до ответа сервера; при конфликте — мягкий откат.
- Бот получает те же события (для мгновенных уведомлений).

---

## 11. Дорожная карта (по этапам)

### MVP (single-tenant, TZ Europe/Moscow) — сайт + бот
- Auth через Telegram (страница авторизации), единый аккаунт бот↔сайт
- Роли и видимость: `super_admin` / `process_lead` / `member` + участники задач; правила «кто ставит задачи»
- Задачи: создание/редактирование (без удаления), приоритеты, **статусы с цветами**, исполнитель, участники, срок+время
- **Подзадачи** и **кастомные поля**
- **Функциональная схема** (дерево функций) + прикрепление задач к функциям
- Проекты и процессы, **любое повторение** (день/неделя/месяц/квартал/дата) по МСК
- Напоминания за 1ч/1д в Telegram
- Доказательства выполнения (текст/ссылка/скрин), блокировка закрытия при `proof_required`; комментарии
- **Поиск, фильтры (как Asana), сортировка** (приоритет/дата/сотрудник)
- **Отчёты**: выполнено/в работе/просрочено + кто просрочивает
- **Входящие** (уведомления о причастных задачах); «Мои задачи» (назначенные + делегированные + участие)
- Веб (ClickUp-стиль): проекты/процессы (канбан/список + карточка) + функциональная схема + входящие + отчёты (+ адаптив/PWA)
- Хранилище файлов self-hosted (MinIO)
- Бот: меню (мои задачи / комментарий / создать / входящие), действия (выполнено / сменить статус / перенести / доказательство), напоминания
- Real-time синхронизация бот↔сайт

### v1.1 (бэклог)
- **ChatPRD**: генерация/улучшение описаний + разбивка на подзадачи
- **Проект = группа/канал Telegram**, обсуждение в Telegram (стиль UTasks)
- Отдельный «Календарь событий», Telegram Mini App для канбана/отчётов

### v1.2 (бэклог)
- Карточка сотрудника со списком задач
- Платёжный календарь
- LLM-чат на сайте, расширенная аналитика загрузки

---

## 12. Открытые вопросы

### ✅ Решено
- **Масштаб** — single-tenant, только наша компания.
- **Часовые пояса** — единый Europe/Moscow.
- **UI** — стиль ClickUp / Asana.
- **Скоуп сайта в MVP** — авторизация + проекты/процессы + функциональная схема.
- **Роли** — `super_admin` (владелец) / `process_lead` (видит все задачи процесса) / `member` (видит только свои задачи) + подключаемые участники задач.
- **Функциональная схема** — управление от функций; задачи прикрепляются к функциям, затем назначается исполнитель.
- **Доказательство выполнения** — свободный формат (текст/ссылка/скрин); требуется по флагу от админа/руководителя; при включённом флаге закрытие задачи блокируется без доказательства.
- **Файлы** — на нашем сервере (self-hosted MinIO).
- **Процесс** — повторение любое (день/неделя/месяц/квартал/дата); поддерживаем постоянные и переменные задачи.
- **Кто ставит задачи** — в проект/процесс/функцию только админ/руководитель; себе — любой сотрудник.
- **Статусы** — взять в работу / в работе / отложено / возникли трудности / не выполнено / выполнено / отменено (с цветами); «просрочено» — производный признак.
- **Отчёты, входящие, поиск/фильтры/сортировка, подзадачи** — входят в MVP.
- **Поля задачи** — базовые (приоритет, исполнитель с аватаркой, статус, срок); доп. кастомных полей по умолчанию не заводим (механизм есть).
- **Функциональная схема** — строит сам админ; модель по образцу **kvant.app** (ОФС).
- **AI** — **ChatPRD** для описаний и подзадач (v1.1).
- **Telegram** — кнопки бота определены; «проект = группа/канал» (UTasks-стиль) — в v1.1.

### Осталось уточнить (не блокирует старт)
1. **Доступ к ChatPRD** — Enterprise API или MCP? Нужен ключ/тариф (для подключения генерации описаний).
2. Всё остальное по продукту — зафиксировано; функции и проекты наполнишь сам в готовой системе.

---

## 13. Следующий шаг

Скоуп и ключевые решения зафиксированы. Дальше:
1. Зафиксировать схему БД (Prisma/Drizzle) — `User`, `Function`, `Project/Process`, `Task` (+ `parent_task_id`, статусы, кастомные поля), `Recurrence`, `Reminder`, `Comment`, `Attachment`, `Notification`, `ActivityLog` — с правилами видимости и запретом удаления.
2. Поднять скелет: монорепо `apps/api`, `apps/web`, `apps/bot`, `packages/shared`.
3. Вертикальный срез MVP: «прикрепил задачу к функции и назначил исполнителя → он видит её в боте, во „Входящих“ и в „Моих задачах“ → сменил статус/перенёс → получил напоминание по МСК → закрыл с доказательством → задача попала в отчёт».
