# TaskFlow

Корпоративный таск-менеджер: **Telegram-бот + веб-кабинет** (single-tenant, TZ Europe/Moscow).
Дизайн в духе ClickUp/Asana, скорость — как Linear. Основная работа ведётся в Telegram, веб — рабочий кабинет.

> Полный продуктовый и технический дизайн — в [DESIGN.md](./DESIGN.md).

## Структура (монорепо, pnpm)

```
taskflow/
├── apps/
│   ├── web/      # Веб-кабинет: React + Vite + TS + Tailwind (дизайн-система + прототип)
│   ├── api/      # Бэкенд: Fastify (скелет; далее Postgres/Prisma, auth, WS, планировщик)
│   └── bot/      # Telegram-бот: grammY (скелет меню и действий)
└── packages/
    └── shared/   # Общие доменные типы и перечисления (статусы, приоритеты, роли…)
```

## Быстрый старт

```bash
pnpm install          # установить зависимости всего монорепо
pnpm dev:web          # веб-кабинет на http://localhost:5173
pnpm dev:api          # API на :3001 (нужен далее)
pnpm dev:bot          # бот (нужен BOT_TOKEN)
```

Сборка веба: `pnpm build:web`.

## Что готово сейчас (итерация 1 — дизайн и структура)

- ✅ Монорепо и общие доменные типы (`@taskflow/shared`)
- ✅ Дизайн-система веба (тёмная тема в стиле Linear)
- ✅ Экраны веб-кабинета (кликабельный прототип на мок-данных):
  - Авторизация через Telegram
  - Мои задачи (назначенные + делегированные + участие)
  - Доска проекта/процесса: **канбан с drag&drop** + список, поиск/сортировка
  - Карточка задачи (статусы с цветами, доказательство, подзадачи, комментарии)
  - Функциональная схема (дерево функций, kvant-стиль)
  - Входящие (уведомления)
  - Отчёты (по статусам + кто просрочивает)
- ✅ Скелеты API (Fastify) и бота (grammY)

## Дальше (следующие итерации)

1. БД и API: Postgres + Prisma, реальные CRUD, auth через Telegram, WebSocket (real-time).
2. Бот: подключение к API, мастер создания задач, напоминания (за 1ч/1д, МСК), доказательства.
3. ChatPRD: генерация описаний и подзадач.
4. Проект = группа/канал Telegram (стиль UTasks), платёжный календарь, календарь событий.
