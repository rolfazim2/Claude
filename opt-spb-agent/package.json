{
  "name": "opt-spb-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Автономный AI-агент разбора оптовых тегов opt-spb.ru (OpenCart-Pro 2.3.0.2)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "agent": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/*.test.ts",
    "postinstall": "playwright install chromium --with-deps || playwright install chromium || true"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.69.0",
    "dotenv": "^16.4.5",
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
